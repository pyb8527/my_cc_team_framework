---
description: Invoke the Frontend Developer agent to handle frontend tasks using React/Next.js skills
argument-hint: <task>
---

Use the frontend-developer agent to handle the following task: $1

The agent specializes in:
- Next.js App Router, Server Components, Server Actions (nextjs-developer, next-best-practices)
- TypeScript + React + Shadcn UI + Tailwind CSS stack (nextjs-react-typescript)
- React performance optimization — bundle size, waterfalls, re-renders (vercel-react-best-practices)

Invoke the agent and let it apply the relevant skills to complete the task.
